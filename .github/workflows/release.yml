name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-macos:
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: aarch64-apple-darwin
            arch: aarch64
          - target: x86_64-apple-darwin
            arch: x86_64

    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install frontend dependencies
        run: npm ci

      - name: Build Tauri App
        run: npm run tauri build -- --target ${{ matrix.target }} --bundles app
        env:
          TAURI_SIGNING_PRIVATE_KEY: ''
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ''

      - name: Get version from tag
        id: version
        run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT

      - name: Create ZIP from app bundle
        run: |
          cd src-tauri/target/${{ matrix.target }}/release/bundle/macos
          zip -r CopyGum_${{ steps.version.outputs.VERSION }}_${{ matrix.arch }}.zip CopyGum.app

      - name: Upload macOS artifact
        uses: actions/upload-artifact@v4
        with:
          name: CopyGum-macos-${{ matrix.arch }}
          path: src-tauri/target/${{ matrix.target }}/release/bundle/macos/CopyGum_${{ steps.version.outputs.VERSION }}_${{ matrix.arch }}.zip

  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable

      - name: Install frontend dependencies
        run: npm ci

      - name: Build Tauri App
        run: npm run tauri build -- --bundles msi,nsis

      - name: Upload Windows MSI
        uses: actions/upload-artifact@v4
        with:
          name: CopyGum-windows-msi
          path: src-tauri/target/release/bundle/msi/*.msi

      - name: Upload Windows NSIS
        uses: actions/upload-artifact@v4
        with:
          name: CopyGum-windows-nsis
          path: src-tauri/target/release/bundle/nsis/*.exe

  create-release:
    needs: [build-macos, build-windows]
    runs-on: ubuntu-latest
    steps:
      - name: Get version from tag
        id: version
        run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: List artifacts
        run: find artifacts -type f

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: CopyGum ${{ github.ref_name }}
          body: |
            ## Downloads

            | Platform | File |
            |----------|------|
            | macOS (Apple Silicon) | `CopyGum_${{ steps.version.outputs.VERSION }}_aarch64.zip` |
            | macOS (Intel) | `CopyGum_${{ steps.version.outputs.VERSION }}_x86_64.zip` |
            | Windows (MSI) | `CopyGum_${{ steps.version.outputs.VERSION }}_x64_en-US.msi` |
            | Windows (Installer) | `CopyGum_${{ steps.version.outputs.VERSION }}_x64-setup.exe` |

            ### Installation
            - **macOS**: Unzip and drag CopyGum.app to Applications
            - **Windows**: Run the installer (.exe or .msi)

            ### First Launch
            Press `Cmd+Shift+V` (macOS) or `Ctrl+Shift+V` (Windows) to open CopyGum.

            ### What's New in ${{ github.ref_name }}
            - Fixed macOS window positioning to properly account for Dock
          draft: false
          prerelease: false
          files: |
            artifacts/CopyGum-macos-aarch64/*.zip
            artifacts/CopyGum-macos-x86_64/*.zip
            artifacts/CopyGum-windows-msi/*.msi
            artifacts/CopyGum-windows-nsis/*.exe
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
